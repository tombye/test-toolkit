#!/usr/bin/env python

    # globals:
    # template github repo = "git@github.com:alphagov/govuk_template"

    # variables:
    # version (version of the template)
    # repo_root (root of this repository)
    # mustache_dir (directory containing the mustache template)

    # make "govuk_template_docs" temporary directory

        # clone the gh-pages branch of the govuk_template into the temporary directory
        # run "git clone -q #{GIT_URL.shellescape} #{dir.shellescape} --branch gh-pages"

        # cd into the govuk_template_docs directory

          # Remove old assets

          # Copy assets folder to current path

          # Load mustache template

          # Loop over all .yml files in "data/"

            # get the file name of the file without the .yml extension

            # Load data file and render mustache with that data

            # swap out all instances of the VERSION_NAME string in the resulting .html page with the new version number

            # Save the rendered file

          # run "git add -A ."
          # run "git commit -q -m 'Publishing govuk_template examples version #{@version}'"
          # run "git push origin gh-pages"